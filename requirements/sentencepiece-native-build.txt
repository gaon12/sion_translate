# Build tools for the separately attested SentencePiece 0.2.1 native overlay.
# These are build inputs, not replacements for the locked training dependency set.
build==1.3.0
cmake==4.1.0
setuptools==80.9.0
swig==4.4.0
wheel==0.45.1
