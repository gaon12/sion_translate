# Python / editors
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
.idea/
.vscode/

# Local Codex state
.agents/
.codex/

# Training data and locally prepared corpora
data/**
!data/.gitkeep
data_mono/
dataset.stale-*/
benchmarks/

# Model weights, tokenizers, checkpoints, and packaged artifacts
artifacts/
artifacts*/
checkpoints/
runs/
exports/
models/
*.pt
*.pth
*.ckpt
*.safetensors
*.onnx
*.model
*.vocab
*.npz
*.bin
*.tar
*.tar.gz
*.tgz
*.zip
*.sha256

# Local evaluation material and service outputs
reports/
comparison_outputs/
glossary_input.json
# Mined from the local game corpora, which are themselves not redistributed.
glossary_mined.json
*.log

# Data acquisition helpers are intentionally kept local. The public project
# starts from user-supplied JSONL and does not redistribute acquisition code.
tools/

# The internal audit. It cites source rows by file and line number, and those
# shards are not redistributed, so the findings only mean anything to someone
# who already has the corpus. Kept local and maintained; see docs/README.md
# for the parts of it that belong in public documentation.
docs/PROJECT_ROAST.md

# Locally generated translation queues contain source and translated text.
# Ignore the complete tree: results and accepted shards live below nested
# directories and must not be staged by a broad `git add .`.
translation_queue/**
